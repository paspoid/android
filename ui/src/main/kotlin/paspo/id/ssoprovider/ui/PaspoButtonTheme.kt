package paspo.id.ssoprovider.ui

/** Visual theme of [PaspoSignInButton]. */
public enum class PaspoButtonTheme {
    /** Filled with the Paspo brand color, white content. The default. */
    BRAND,

    /** Light background, dark content, subtle border. */
    LIGHT,

    /** Dark background, light content. */
    DARK,

    /** Filled neutral-grey background, dark content. */
    NEUTRAL,
}
